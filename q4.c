#include<stdio.h>

int main()
{
 int n,a,i,s;
    do
    {
        scanf("%d",&n);
    } while(n<1 || n>1000);
    for(i=0;i<=a;i++)
    {
    if(n%i==0)
    {
        s++;
    }
    if(s==2)
    {
        printf("%d",);
    }
    }
    return 0;
}